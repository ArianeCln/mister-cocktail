<h5 class=""><strong>Add a new Dose</strong></h5>
<%= simple_form_for [@cocktail, @dose, @ingredient] do |f| %>
<%= f.input :description %>
<%= f.input :ingredient %>
<%= f.button :submit, class: "btn btn-primary btn-block" %>
<% end %>
